// "Majestiqos" is the brand name — not translated (brand names never are),
// so it lives here rather than in the message files.
export const siteConfig = {
  name: "Majestiqos",
  // Placeholder — no real Instagram account exists yet.
  instagramHandle: "majestiqos.store",
} as const;
